# opkg update
# opkg install qos-scripts

Configure qos-scripts accordingly (https://wiki.openwrt.org/doc/uci/qos)

# cp QoSMLib.lua /usr/lib/lua/QoSMLib.lua
# mkdir /usr/share/QoSM/
# touch /usr/share/QoSM/QoS_default
# touch /usr/share/QoSM/QoS_high
# touch /usr/share/QoSM/QoS_priority
# touch /usr/share/QoSM/QoS_express
# cp /etc/hosts /etc/hosts.backup
# cp /etc/ethers /etc/ethers.backup

To create the IP address ranges:

# cp qos /etc/config/qos

These ranges fit a special network environment where the need for this lib came from. If you modify the ranges, also modify the lib itself.

